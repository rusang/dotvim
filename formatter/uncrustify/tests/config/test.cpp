#include "my.h"
#include <iostream>

int main(int argc, char* argv[])
{
    int a = 0;
    for (a = 0; a < 9; a++) {
        printf("hello world\n");
    }
    double b = 0.0;
    double c = a + b;
    if (c != 0) {
    }
    for (b = 0; b < 9; b--) {
        printf("this is fuck");
    }
    if ((a + b) / c) {
    }

    return 0;
}
